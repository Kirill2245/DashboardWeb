import styles from './styles.module.css';

const Messages = () => {
    return(
        <section className= {styles.section}>
            <>MMM</>
        </section>
    );
};

export default Messages